/// Game state enumeration
enum GameState {
  loading,
  mainMenu,
  playing,
  paused,
  gameOver,
  settings,
}

/// Difficulty levels
enum DifficultyLevel {
  easy,
  medium,
  hard,
  nightmare,
}

/// Audio volume levels
enum VolumeLevel {
  muted,
  low,
  medium,
  high,
}
